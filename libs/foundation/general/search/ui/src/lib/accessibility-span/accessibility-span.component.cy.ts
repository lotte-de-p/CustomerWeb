import { AccessibilitySpanComponent } from './accessibility-span.component';

describe('AccessibilitySpanComponent', () => {
  it('should render', () => {
    cy.mount(AccessibilitySpanComponent);
  });
});
