import { S as SIZES } from './sizes.types.js';

const BUTTON_APPEARANCES = {
    primary: 'primary',
    secondary: 'secondary',
    tertiary: 'tertiary',
};
const TYPES = {
    button: 'button',
    submit: 'submit',
    reset: 'reset',
    file: 'file',
};
const BUTTON_ELEMENTS = {
    button: 'button',
    input: 'input',
    a: 'a',
};
const BUTTON_SIZES = {
    md: SIZES.md,
    sm: SIZES.sm,
};
const BUTTON_WIDTHS = {
    none: 'none',
    full: SIZES.full,
};
const BUTTON_STATUSES = {
    none: 'none',
    disable: 'disable',
    loading: 'loading',
    skeleton: 'skeleton',
};
const BUTTON_ICON_POSITIONS = {
    left: 'left',
    right: 'right',
};

export { BUTTON_ELEMENTS as B, TYPES as T, BUTTON_APPEARANCES as a, BUTTON_SIZES as b, BUTTON_WIDTHS as c, BUTTON_STATUSES as d, BUTTON_ICON_POSITIONS as e };

//# sourceMappingURL=button.types.js.map