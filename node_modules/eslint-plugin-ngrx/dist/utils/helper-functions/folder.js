"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.traverseFolder = void 0;
const fs_1 = __importDefault(require("fs"));
const path_1 = __importDefault(require("path"));
function* traverseFolder(folder, extensions = ['.ts', '.js']) {
    const folders = fs_1.default.readdirSync(folder, { withFileTypes: true });
    for (const folderEntry of folders) {
        const entryPath = path_1.default.resolve(folder, folderEntry.name);
        if (folderEntry.isDirectory()) {
            yield* traverseFolder(entryPath, extensions);
        }
        else if (extensions.includes(path_1.default.extname(entryPath))) {
            yield { folder, file: path_1.default.parse(folderEntry.name).name, path: entryPath };
        }
    }
}
exports.traverseFolder = traverseFolder;
