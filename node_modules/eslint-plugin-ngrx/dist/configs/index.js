"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.configs = void 0;
const utils_1 = require("../utils");
const interopRequireDefault = (obj) => (obj === null || obj === void 0 ? void 0 : obj.__esModule) ? obj : { default: obj };
const importDefault = (moduleName) => interopRequireDefault(require(moduleName)).default;
const configDir = __dirname;
const excludedFiles = ['index'];
exports.configs = Array.from((0, utils_1.traverseFolder)(configDir))
    .filter((config) => !excludedFiles.includes(config.file))
    .reduce((allConfigs, config) => {
    const entry = importDefault(config.path);
    return Object.assign(Object.assign({}, allConfigs), { [config.file]: entry });
}, {});
