"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.isStencilProject = exports.isStencilProjectBuilder = void 0;
function isStencilProjectBuilder(project, builderCommand) {
    const buildArchitect = project.architect && project.architect[builderCommand]
        ? project.architect[builderCommand]
        : {};
    return (buildArchitect &&
        buildArchitect.builder === `@nxext/stencil:${builderCommand}`);
}
exports.isStencilProjectBuilder = isStencilProjectBuilder;
function isStencilProject(project) {
    return ['build', 'test', 'serve', 'e2e']
        .map((command) => {
        const target = project.targets && project.targets[command]
            ? project.targets[command]
            : {};
        return target && target.executor === `@nxext/stencil:${command}`;
    })
        .some((value) => value === true);
}
exports.isStencilProject = isStencilProject;
//# sourceMappingURL=migration-utils.js.map