import { StencilTestOptions } from './schema';
import { ExecutorContext } from '@nx/devkit';
export default function runExecutor(options: StencilTestOptions, context: ExecutorContext): Promise<{
    success: boolean;
    error?: undefined;
} | {
    success: boolean;
    error: any;
}>;
