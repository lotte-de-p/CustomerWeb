import { StencilBuildOptions } from './schema';
import { ExecutorContext } from '@nx/devkit';
export default function runExecutor(options: StencilBuildOptions, context: ExecutorContext): Promise<{
    success: boolean;
    error?: undefined;
} | {
    success: boolean;
    error: any;
}>;
