import { ConfigFlags, TaskCommand } from '@stencil/core/cli';
import { PathCollection } from './types';
import { ExecutorContext } from '@nx/devkit';
import type { Config } from '@stencil/core/compiler';
export interface StencilBaseConfigOptions {
    configPath: string;
    outputPath: string;
    tsConfig?: string;
}
export declare function initializeStencilConfig<T extends StencilBaseConfigOptions>(taskCommand: TaskCommand, options: T, context: ExecutorContext, flags: ConfigFlags): Promise<{
    pathCollection: PathCollection;
    strictConfig: Config;
}>;
