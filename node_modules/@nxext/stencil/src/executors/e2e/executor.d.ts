import { StencilE2EOptions } from './schema';
import { ExecutorContext } from '@nx/devkit';
export default function runExecutor(options: StencilE2EOptions, context: ExecutorContext): Promise<{
    success: boolean;
    error?: undefined;
} | {
    success: boolean;
    error: any;
}>;
